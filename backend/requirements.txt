# Atlas backend — runtime dependencies only.
# Trimmed from the old Emergent base image: removed pandas, numpy, huggingface,
# tokenizers, openai, litellm, stripe, and the dev/lint tools (black, mypy,
# flake8, pytest). pip resolves all transitive dependencies from these.

# Web framework + server
fastapi==0.110.1
uvicorn==0.25.0
python-multipart==0.0.27       # required by FastAPI for file uploads / form data
python-dotenv==1.2.2

# Database
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0               # required for mongodb+srv:// (Atlas) connection strings

# Validation
pydantic==2.13.3

# Object storage (Cloudflare R2 / AWS S3)
boto3==1.43.0

# AI (Google Gemini) + Gmail OAuth
google-generativeai==0.8.6
google-api-python-client==2.194.0
google-auth==2.49.2
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.3.1
requests==2.33.1               # used by google.auth.transport.requests
